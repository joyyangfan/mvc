version: '2'
services:
    dotnetcore:
        container_name: mymvc.web.compose
        # image: ':latest'
        build: .
        restart: always
        #environment:
        ports:
          - '5001:5000'
        command: MyMvc.Web.dll
        #volumes:
          # - '/data/:/publish/data'