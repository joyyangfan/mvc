version: '2'
services: 
    dotnetcore: 
        container_name: mymvc.web.compose
        # image: ':latest'
        build: .
        restart: always
        environment:
          - ASPNETCORE_ENVIRONMENT=Staging
        ports:
          - '5555:5000'
        command: MyMvc.Web.dll
        volumes: 
           - /log/:/publish/log:rw
